import post from './post'
import {amenitiesObject, sliderImageObject, unavailablePeriodObject, accommodationSchema} from './accommodations'

export const schemaTypes = [post, amenitiesObject, sliderImageObject, unavailablePeriodObject, accommodationSchema]
